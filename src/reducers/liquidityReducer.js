const initialState = {
    isFetching: false,
    data: []
}

const liquidityReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
          return state; 
    }
}

export default liquidityReducer;